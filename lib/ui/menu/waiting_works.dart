
import 'package:flutter/material.dart';


class WaitingWorks extends StatelessWidget {
  const WaitingWorks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Waiting Works <---"),
      ),
    );
  }
}
